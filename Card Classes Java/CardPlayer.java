public class CardPlayer extends Player {
    public CardPlayer(String ip, int score, int playerNumber) {
        this.setIp(ip);
        this.setScore(score);
        this.setPlayerNumber(playerNumber);
    }

}